name: CD workflow
run-name: ${{ github.actor }} is learning CD workflow in Github Actions
on:
  push:
    branches: [ main ]
  workflow_dispatch:
#    inputs:
#      environment:
#        description: 'Select environment'
#        required: true
#        default: 'dev'
#        type: choice
#        options:
#          - dev
#          - staging
#          - production

jobs:
  generate-apk-build:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: cicd
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Java
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '21'

      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: 'wrapper'

      - name: Build debug apk
        run: ./gradlew assembleDebug

      - name: Upload debug apk
        uses: actions/upload-artifact@v4
        with:
          name: debug-apk
          path: cicd/app/build/outputs/apk/debug/*.apk

  send-apk-to-email:
    needs: generate-apk-build
    runs-on: ubuntu-latest
    steps:
      - name: Send debug apk to email
        uses: dawidd6/action-send-mail@v3
        with:
          server_address: smtp.gmail.com
          server_port: 465
          username: ${{ secrets.EMAIL_USERNAME }}
          password: ${{ secrets.EMAIL_PASSWORD }}
          subject: New Debug APK file
          to: zeel.chetan@anywhere.co
          from: GitHub Actions <${{ secrets.EMAIL_USERNAME }}>
          body: |
            Hi
            This is the new debug APK file generated from the CI/CD workflow using github actions.
            
             👉 You can download it from the GitHub Actions artifacts here:
            ${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}